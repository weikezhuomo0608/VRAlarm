# 更新记录

## 1.1.0（首个公开版本）

- 本仓库首个公开版本，并入内部迭代 1.1.0–1.4.3 的全部能力：多主播守候与接续响铃、周表（手动/文字识别/AI 识别/图片对照）、开播统计、桌面小组件、外观自定义（主题色/AMOLED/自选背景）、烘焙圆角的满幅位图图标、定时恢复检查等；完整变更明细见下方内部版本记录。
- 应用不再内置 AI 接口密钥：需在「设置 → AI 图片识别」自行填写 DeepSeek 兼容密钥。
- 应用显示名更名为「VR闹钟」（包名不变；下方历史条目中的「主播闹钟」为曾用名）。
- 仓库已清理私人数据与签名材料路径；上游 release 产物不入库。


## 1.4.3

- **开源准备**：设置里的 AI 接口密钥不再有内置默认值——应用出厂为空，需在「设置 → AI 图片识别」自行填入你自己的 DeepSeek 兼容密钥（仅保存在本机，导出备份不含密钥）。此前从旧版升级的设备不受影响，重装后需重新填一次。
- 移除了测试工具与内部文档里的私人数据（真实主播 UID/房间号、签名材料路径）。

## 1.4.2

- **图标再修**：构图恢复到与原版一致——人物向下裁到小腿（有部分腿），不再只有上半身；圆角不再依赖桌面蒙版，直接烘焙进图片的透明圆角里，任何桌面都显示为无棱角圆角框。
- **移除「从置顶动态获取」**：B 站接口风控导致该功能成功率没有保障，按反馈整体下线（按钮、动态接口与 Wbi 签名代码一并移除）。周表仍支持：手动逐条添加、粘贴文字识别、上传图片对照、AI 识别。
- 修复设置页「数据与帮助」列表排版：行右侧箭头错误地参与了弹性布局，把文字挤到折行；现在每行单行显示、箭头靠右。

## 1.4.1

- **图标改为满幅位图**（接 1.3.6）：荣耀等定制桌面会把自适应图标压平到白色底板上显示（直角、人物缩小），且缓存严重。现改为与旧版满区闹钟一致的做法——渐变底 + 上半身喇叭立绘直接烘焙成全出血方形位图，全密度提供（48–192px），由桌面自行裁圆角。安装后若图标未刷新，请**先卸载、重启手机、再安装**一次以清空桌面图标缓存。

## 1.4.0

- 移植原版满区闹钟 1.0.6 的四项设计（Webview 架构下的等价实现；原版的 Compose 原生界面本身不适用）：
- **自选背景图片**：设置 → 背景图片，选一张喜欢的图作应用背景（≤20 MB，自动缩放，仅保存在应用内）；「背景遮罩」压暗背景保证文字可读，「卡片不透明度」控制透出程度。响铃页保持原有设计不显示背景。
- **主题色**：预设 12 色色板 + 自定义 HEX，明暗两套按种子色自动推导（与原版同一公式）；**AMOLED 纯黑模式**：深色主题下使用纯黑底色。
- **从最近任务隐藏缩略图**（默认关）：隐藏最近任务卡片里的应用内容，减少误划与窥屏；不影响提醒。
- **定时恢复检查**（默认开）：每约 15 分钟用「经过时间」闹钟（不受系统时间跳变影响）确认守候服务是否还活着，中断时自动拉起并写入记录——与原有的保活、看门狗、无障碍辅助互为第四层保险。设置页显示下次恢复检查时间。
- 备份仍不包含背景图片与自选铃声；导出继续剥离 AI 密钥。

## 1.3.7

- **主播卡片排版修复**：UID/直播间号与底部统计按整段折行，「直播间」「1 场」不再被劈成两半（如「直/播间」「本周 1/场」）；统计文字移到分隔线正下方独立一行，「周表/编辑/直播间/空间」按钮单独一行，垂直对齐统一；「编辑」图标由箭头改为铅笔，与周表（日历）、直播间/空间（外链）的语义图标风格一致。

## 1.3.6

- **修复桌面图标**：此前图标前景直接复用 3.5 MB 全尺寸立绘，部分桌面（荣耀等）解码困难时会退化成「白底直角方块、人物小」的未加工观感。现按旧版满区闹钟的样子重新加工：裁取上半身放大（喇叭贴嘴、齐裙截断）、抠除白底、背景改为浅灰白渐变，前景降为 120 KB 独立资源；APK 因此瘦身约 3.5 MB。若安装后桌面图标未立刻刷新，重启一次桌面（或卸载重装）即可。
- 说明：公开仓库与 1.0.5 公开包里从来只有未加工的原始立绘，旧桌面上的成品图标来自作者本地构建，无法直接还原，本版为按观感重制。
- 响铃页与周表使用的 `assets/hazel.png` 插画不受影响。

## 1.3.5

- 响铃页恢复使用原先的 hazel 插画：1.3.2 起封面图会在响铃时替换插画，直播间截图在这一页观感差；封面缩略图保留在周表时间线上。
- 新增**桌面小组件**：守候状态、正在直播的主播、下一次周表预告与上次检测时间，一目了然；每轮检测与心跳都会刷新，纯本机数据。
- 发布前自查修复七处：预告闹钟的时间计算从「只看今明两天」改为「扫整周」（周六本来看不到下周五的场次）；预告通知与小组件显示的是开播时刻而非提醒时刻（差 5 分钟）；导出备份剥离 AI 密钥（避免误分享）；AI 识别 max_tokens 提高到 4000（推理模型可能耗尽配额导致空回复）；封面代理非 200 时断开连接；重启后小组件立即刷新一次。
- 注意：早先打包的 1.3.1–1.3.4 安装包含上述小问题，已被本版取代，请直接安装 1.3.5。

## 1.3.4

- 新增**多主播接续响铃**（声音页开关，默认关）：开启后，第一位响铃期间开播的其他主播按顺序接续提醒，而不是只记录；手动关闭响铃会跳过整条队列。
## 1.3.3

- 新增**开播统计**：按主播记录每场开播与结束（本机保留最近 60 场），主播卡片显示「本周 N 场 · 近 30 天 N 场」。
## 1.3.2

- 新增**直播间封面**：正在直播的主播在周表时间线与响铃页显示直播间封面（检测接口本就返回，无新增请求；封面经应用自身代理加载，仅限 B 站图床域名，界面仍不直连外网）。
## 1.3.1

- 新增**即将开播自动提速检测**：周表显示某主播 30 分钟内要开播（含开播后 30 分钟）时，即使处于提醒时段外，检测间隔也从约 3 分钟的低频自动提回你设置的间隔，过后自动恢复省电；守候通知会显示「临近周表开播 · 高频检查中」。
- 新增**按周表预告提醒**（时段页开关，默认开）：周表开播前 5 分钟发一条「快开播了」提醒通知，不响铃；仅对已开启响铃的主播生效。

## 1.3.0

- 新增 **AI 识别周表图片**：上传周表图片后，在周表编辑弹窗点「AI 识别图片安排」，AI 自动提取“星期|开始|结束|备注”并进入确认列表，确认后才保存——与文字识别共用同一套人工把关。
- 已用 DeepSeek `deepseek-flash`（视觉）真接口端到端验证：中文周表截图 → 结构化安排行，无时间的行（如“休息”）正确跳过，单张约 577 tokens。
- 设置页新增「AI 图片识别」区块：开关（默认开）、接口密钥与模型名（需支持图片输入）。密钥仅保存在本机。
- **隐私变化（默认开启，请知悉）**：点按 AI 识别时，该张周表图片会经压缩后上传到你配置的 AI 服务商处理；不点按不上传。可随时在设置中关闭。
- 超过 1.5 MB 的图片先缩放到约 1280 px 再上传（识别文字足够，流量更小）。
- **内置的默认接口密钥仅供个人使用：若把本仓库公开或推送，请先在设置中更换密钥并从 `Prefs.java` 的默认值中移除。**
- 识别失败会显示服务商返回的具体原因；流程失败不写任何数据，不影响手动录入。

## 1.2.2

- 新增「刷新全部头像」：主播页一键为所有主播重新获取头像。此前头像只在识别时下载一次，当时失败（网络抖动或限流）就永远是首字母占位——这正是“部分主播没有头像”的原因。
- 置顶动态抓取重写：B 站已下线旧版免登录动态接口（HTTP 404 的直接原因），改为带 Wbi 签名的现行动态接口（导航接口取密钥 + 访客凭证），并保留粘贴动态链接的读取。**新接口仍受 B 站风控影响，失败时照旧指向手动粘贴或上传图片。**
- 周表图片改为「对照模式」：上传的图片直接显示在编辑列表上方，边看图边逐条录入。

## 1.2.1

- 新增「从置顶动态获取」：在周表编辑弹窗里一键读取该主播置顶动态的文字，填入来源框后点「识别为安排」进入确认流程；也支持直接粘贴 `t.bilibili.com/<动态id>` 链接抓取单条动态。
- 实现走免登录的旧版动态接口并做了多层数据形态兼容；**接口随时可能被 B 站风控拦下（-352 等）**，失败时会明确提示，转手动粘贴或上传图片，不影响任何已有功能。
- 读取置顶动态会让 B 站服务收到常规请求信息，已同步写入隐私说明。

## 1.2.0

### 响铃独立开关

- 每位主播从单个开关变为两个：「检测」决定是否检测（关掉即完全不看）；「响铃」决定检测到开播时是否响铃——关闭后照常检测、时间线照常亮起，只在记录里写一条「响铃已关闭」。
- 响铃关闭的场次被标记为已处理：中途重新打开响铃，不会为已经播了一半的场次突然补响，下一场才生效；也不会占用「同一时间只响一次」的坑位。
- 新增主播弹窗里也有「开播响铃」开关（默认开）；备份恢复、旧版本数据迁移均兼容（旧数据默认响铃开）。
- 自检发现并修复：仅切换「检测」开关时响铃开关会被重置回开启；响铃状态现在在缺省时保留原值。

### 周表与时间线

- 新增「周表」页（导航第 6 个标签）：按周一分组展示各主播的直播安排，今天在最前并标注，正在直播的主播亮绿色圆点，跨午夜结束时间按次日理解。
- 每位主播最多 16 条安排（星期位掩码 + 开始时间 + 可选结束时间 + 备注）；数据模型为纯 Java 校验（20 条断言）+ 容错读取，坏条目丢弃不致命。
- 编辑弹窗三种来源任选：逐条添加（星期选择器 + 时间 + 备注）、粘贴周表文字自动识别（宽松匹配「周X/星期X/礼拜X/周末」+「20:00/20点/下午2点」等写法，识别结果进列表人工确认后才保存）、上传周表图片（仅展示，明确不做 OCR）。
- 周表原文保留在本机，重新打开编辑器无需重新抓取；周表只用于展示，是否响铃仍由「时段」页与响铃开关决定，两套时间规则互不干扰。
- 更换房间号会清空该主播的周表（周表属于直播间）；删除主播时周表、原文与图片一并清除。
- 自检发现并修复：周表编辑弹窗重绘会清空正在输入的来源文字；重绘现在保留当前输入。

## 1.1.2

- 修复：**主播头像从来没有显示过**。界面用相对路径请求 `avatar/<id>.png`，实际落在 `/assets/avatar/…`，而应用自身的头像路由只认 `/avatar/…`，请求被打到不存在的打包资源上返回 403，界面只好退回首字母占位。已改为绝对路径；界面测试的服务器现在提供真实头像文件，头像能否渲染从此被场景真正覆盖。
- 新增：编辑主播弹窗里的「重新下载头像」按钮，头像下载失败或主播换过头像时无需删除重加。
- 新增：没有头像时的首字母占位改为按主播编号稳定取色的渐变底色，不再是纯灰块。

## 1.1.1

真机反馈：从最近任务划掉应用后，守候仍不恢复。复查后确认这是系统的强杀行为，不是可以靠应用代码否决的操作（这一限制对已发布的 1.0.5 同样成立，且 1.0.5 连恢复用的闹钟都没有）。本版做两件能做的事：把中断如实记下来，并把真正管用的设置讲清楚。

- 新增中断记录：检测实际开始时间比服务自己安排的时间晚 5 分钟以上、且当时处于提醒时段内时，写入一条「守候曾被中断」记录，附上晚了多久与要检查的设置。这是区分「恢复用的闹钟根本没被投递」和「闹钟投递了但系统拒绝启动服务」的唯一证据——两者症状相同，修法完全不同。
- 守候页的中断提示补充了最容易被漏掉的一步：在最近任务里长按本应用卡片将其**锁定**。从最近任务划掉应用时，系统会连同恢复用的闹钟一起停掉，只配自启动与电池不限制仍然不够。
- 守候页的中断提示新增「开启无障碍守候辅助」入口（未启用时显示）。无障碍服务由系统自己重新绑定，因此系统清理本应用后仍会把它拉起来，再由它恢复守候——这是划掉后台后恢复最快的一条路；说明文案也写清了这一点与前提条件。
- 新增：荣耀 MagicOS 的设置路径写进设置页与使用说明（应用启动管理 → 关闭自动管理 → 勾选自启动、关联启动、后台活动）。
- 修复：系统反复拒绝后台启动时，「守候未能启动」记录会按保活闹钟的节奏（约 90 秒一条）刷满记录页；现在同类失败最多每 10 分钟记一条。
- 修复：前台服务未能进入前台时，保活闹钟不再被遗留下来（现在只在服务成功进入前台后才安排）。

验证：40 个界面与模拟通信场景通过（中断提示场景扩展为同时校验最近任务指引、无障碍入口与说明文案）；核心规则断言 36038 + 12 + 13 + 46 + 72 条通过；24 个源文件括号配平通过。APK 已用 Android SDK 35 重新编译、D8、zipalign 并通过 apksigner v2/v3 校验，`versionCode=8`、`versionName=1.1.1`。**仍未在真机验证。**

## 1.1.0

- 新增“主播”页：填入 B 站直播间号码即可自动识别主播名称与头像，最多 16 位主播，可增删、改名、分别开关。
- 应用显示名改为「主播闹钟」（启动器名称、界面标题、通知标题与无障碍服务名一并统一）；仓库与文档中的项目名仍为满区闹钟。
- 守候改为按主播逐一检测：每位主播各自记录场次去重状态，同一时间只响一次铃，其余主播开播时写入记录而不叠加响铃。
- 提醒时段与铃声由所有主播共用；每位主播只有独立的启用开关。
- 头像在识别时下载一次并缓存到应用私有目录，由应用自身提供，界面不向外部网络直接请求图片；识别失败时退回首字母占位。
- 轮询随主播数量调整：唤醒锁覆盖整轮检测，单轮耗时计入间隔，避免主播变多后检测明显滞后。
- 限流退避按整轮判断：任一主播触发 B 站限流即放慢整轮重试，并把失败主播计为一次失败，不再逐主播静默重试。
- 备份与恢复包含主播列表；删除主播时一并清除该主播的场次状态与缓存头像。
- 升级时自动沿用原有单主播的场次状态，不会在升级后重复提醒同一场直播。

### 全天守候的可靠性

- 新增检测保活闹钟：每轮检测结束后，按本轮间隔的三倍安排一次系统唤醒（下限 90 秒）。检测循环跑在主线程消息队列上，本身没有唤醒能力，设备休眠或厂商后台限制会让它悄悄停住；保活闹钟是唯一能在用户不开应用的情况下把它重新拉起来的东西。循环正常时每轮都会把它推后，因此它只在真的停住时才触发。
- 新增守候看门狗：每 15 分钟由系统闹钟检查一次服务心跳，服务已停就在后台尝试自行恢复。此前全天守候不安排任何定时唤醒（时段边界只对自定义时段生效），进程被系统杀掉后只能靠重启手机或手动打开应用才会恢复——这是“整天没有提醒”的直接原因。
- 新增异常兜底：单次检测中的内部错误不再让整条轮询静默终止，也不再以未捕获异常结束进程；错误记入记录后仍按间隔重试。
- 新增：守候页在服务心跳超过 3 分钟未更新时明确提示“守候服务已中断”，并给出重新开启与自启动、电池不限制的设置指引。
- 修复：检测成功时没有清除“网络未连接，联网后会自动重试”提示，一次网络波动后该提示会一直留在守候页。现在与 1.0.5 一致，成功检测即撤下提示。
- 修复：Wi-Fi 与移动数据切换的瞬间可能被误判为断网；现在只要存在任一可用网络就不再算断网，避免误报并避免无谓地放慢重试。
- 修复：后台启动被系统拦下后留下的提示会一直挂在守候页；现在启动类失败单独记录，服务起来或检测成功即撤下，不再与“通知被拒”提示混用同一个位置。

### 正确性

- 修复：新增主播、或给已有主播换房间号时，会沿用该编号上一次的场次去重状态；现在这类改动会重置该主播的场次与上次状态，并把它的“加入时间”记为当前时刻——加入时已经在播的直播不会再被当成刚开播而误响。
- 修复：与界面文案不一致的互斥响铃。此前第二位主播开播只写一条记录并不响铃，但那一场并没有被标记为已提醒，于是第一位主播的响铃一停，下一次检测就会接着响——正是文案所承诺不会出现的“叠着响”。现在被跳过的场次会标记为已处理，只记入记录。
- 新增：真实开播可以打断正在进行的响铃测试（此前测试响铃期间检测到的开播会被当作重复而丢弃）。
- 修复：直播间身份校验同时比对了房间号与主播 UID，而 B 站两个接口对同一房间可能给出不同的 UID，一旦不一致该主播就永久无法提醒。现在只按房间号校验，UID 仅用于跳转个人空间。
- 修复：填入不存在的房间号时提示“B 站接口返回 -400，等待重试”，现在提示“没有找到这个直播间，请检查房间号”。
- 修复：发布版号在多个文件里写死，改一次版本要改多处且容易漏。界面页脚与通知排查包改为从已安装的包信息读取真实版本号，不再有写死的版本常量。

验证：40 个界面与模拟通信场景通过（含 8 个主播页与守候状态场景）；核心规则断言 36038 + 12 + 13 + 46 + 72 条通过；24 个 Android 源文件括号配平通过。Android 端 Java 已用 Android SDK 35 与 build-tools 35.0.0 完整编译，经 D8、zipalign 并通过 apksigner v2/v3 签名校验，产物为独立包名 `dev.hazel.livealarm.multi`（应用名「主播闹钟」，可与原版并存），`versionCode=7`、`versionName=1.1.0`。**尚未在真机运行**，范围见 [测试记录](docs/TESTING.md)。

## 1.0.5

- 守候通知固定为前台服务通知，响铃使用另一条通知，结束响铃后继续保留守候状态。
- 通知提供立即检测、停止守候等操作，响铃通知提供停止与稍后提醒操作。
- 授权状态发生变化或回到应用时重新读取权限与通道状态，并更新界面。
- 设置页分别显示服务运行、系统授权、通道状态和通知登记情况，避免把服务运行误写成通知已显示。
- 停止守候直接撤销检测和相关定时任务；不再为了执行停止动作而新建服务。
- 前台服务临时启动失败时保留先前的守候设置；开启守候时的测试结束后继续正常检测。
- 保留 1.0.3 的显式同意兼容响铃，以及之前的铃声选择和设置即时刷新修复。

验证：30 个界面与模拟通信场景通过；13 个通知状态与 12 个响铃策略断言通过。原生测试 APK 已编译，尚未由本项目验证环境在真机执行；完整范围见 [测试记录](docs/TESTING.md)。

通知开关被设备管理策略固定拒绝时，需调整对应管理策略。本版识别并解释该状态，不宣称可以绕过它。

## 1.0.4

- 识别系统策略拒绝通知的状态，避免重复请求已被策略阻止的运行时授权。
- 增加用户主动导出的通知诊断与可选系统权限组件导出。

## 1.0.3

- 应用名称统一为“满区闹钟”。
- 新增“通知异常时仍响铃”，须由用户明确开启。
- 增加响铃时可选的悬浮停止入口，普通使用不依赖外部授权工具。

## 1.0.1–1.0.2

- 修复设置保存和异步状态刷新竞态：铃声、开关、主题等选择无需切页即可显示。
- 修复只有权限变化时界面不更新的问题，增强返回应用后的授权读取和系统设置入口。
