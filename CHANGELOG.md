# 更新记录

## 1.1.0808（满区版）

**这一版的主要变化是把「桌面小宠物」正式对外发布**，其余为 1.1.6 之后本地版本积累的修复与打磨。GitHub 上标为 **1.1.0808**，安装包内的版本号仍是 `versionName 1.1.8`／`versionCode 40`。

- **时段之外不再进行任何形式的检测**（2026-09-15 修订，版本号有意不变）：从前「时段」只决定**要不要响铃**，时段之外仍会以约 3 分钟一次的节奏继续联网检查开播状态。现在时段决定**是否守候**——进入时段之外会**自动暂停守候**：检测循环停止、保活与定时恢复检查一并撤销、唤醒锁释放、前台服务停止（常驻通知随之消失），**不联网、不判读直播状态**；进入下一个时段时自动恢复。
  - 恢复时刻由**不推迟的闹钟式唤醒**（`setAlarmClock`）按点触发，避免「夜里晚 9 分钟才开始监测」；代价与「持续高频守候」相同：状态栏会出现闹钟图标，且需要「精确闹钟」权限（未授予时自动退回较弱、会被系统推迟的唤醒）。
  - 首页会显示「时段外 · 守候已暂停」并写出恢复时刻；这条**不再被误报成「守候服务已中断」**（那两件事现在分开显示）。
  - **自定义时段里一条启用中的时段都没有**时，守候会一直暂停（这是配置问题，不是故障），首页与「时段」页都会明确提示并给出一键改为全天提醒的入口。
  - 时段语义不变：起点包含、终点不包含；跨午夜时段按**开始那天**的星期计算；多条时段取并集。
- **修复打开「通知与运行记录」时闪一下屏**（2026-09-16 修订，版本号不变）：这一页的记录要过桥读取，原来的写法是先把页面骨架写上去、等回包再把列表填进去 —— 中间会**真实绘制一次「列表还是空的」的画面**；记录多时（最多 200 条）这一帧会长到能看见，那一下闪就是它。现在改成先读再画，并把读到的记录留在内存里：进入页面只画一次，之后的刷新**只替换列表、且只在记录真的变了时才替换**；清空记录会一并丢弃缓存。
- **修复打开「通知与运行记录」时闪一下屏**（2026-09-16 修订，版本号不变）：这一页的记录要过桥读取，原来的写法是先把页面骨架写上去、等回包再把列表填进去 —— 中间会**真实绘制一次「列表还是空的」的画面**；记录多时（最多 200 条）这一帧会长到能看见，那一下闪就是它。现在改成先读再画，并把读到的记录留在内存里：进入页面只画一次，之后的刷新**只替换列表、且只在记录真的变了时才替换**；清空记录会一并丢弃缓存。

- **新增桌面小宠物「满区」**：沿底部独立的一栏来回走，偶尔自己跳一下；点头时带动围脖（两层硬切图，头绕脖子摆动、围脖反向小角度跟随），向右走时整体镜像。默认开启，可在「设置 → 桌面小宠物」关闭（关闭后底部留白自动收回），款式可在「满区」与「绿冻」之间切换。
- **形象来源**：「满区」与「绿冻」的形象来自 B 站 up 主 **贰三3三** 的视频 **《大大方方的桌泽满灰宠》** <https://www.bilibili.com/video/BV1mMuu6wESX/>。本项目只是把该形象做成应用内的小宠物，**形象版权归原作者**；若作者不希望被这样使用，请联系我下架——本版以「满区版」命名，一是庆祝它终于进到应用里，二是把出处写在明面上。
- 小宠物只在应用可见时活动（页面隐藏即停止动画），不联网、不占后台、点击穿透，不会挡住任何按钮。
- 其余改动（相对对外 1.1.6）：
  - 首页「打开直播间」改为**两段式**：先展开主播列表（正在直播的排在前面），再点要进的那一位，主播多时列表自身限高滚动；
  - **周表只有「今天、已轮到的那一场」显示「正在直播」**，其余显示「计划开播」（此前该主播在播时，整周每一条都会显示正在直播）；
  - **周表图片替换后立即生效**（此前换图后仍显示第一张，需要重启应用）；
  - **全屏提醒页的「去直播间 · 关闭响铃」一次点按就会打开直播间并结束响铃**（此前这个按钮没有反应，只能先关掉铃声、再自己去找直播间）；
  - 宠物栏按实际导航栏高度计算贴位，字号更大或不同机型下都与导航栏相接，不再悬空。
- 版本号：安装包 `versionName 1.1.8`、`versionCode 40`（高于对外 1.1.6 的 35），可直接覆盖升级；对外显示为 1.1.0808。
- 本次含一处对**提醒链路**的改动（全屏提醒页的按钮接线）：按项目约定先取得明确许可，并补了回归场景「the ringing page opens the named room in one tap」；`LiveGate`、`Prefs.armedAt()`、响铃与通知、时段语义、闹钟编号均未改动。

## 1.1.7（本地版：公开 1.1.6 + 桌面小宠物）

**这一条是本地试装记录，内容已并入上面的 1.1.0808。** 内容 = 对外 1.1.6（含「持续高频守候」等可靠性改进）+ 桌面小宠物。小宠物现在已随 1.1.0808 对外发布，并注明形象出处。

- **满区改成按桌宠实际形象重做**：此前是从桌面版的 Spine 骨骼离线烘焙，渲染出来是**歪着的**（那套骨骼有 27 条物理链，本机渲染器只能把它们钉在静止姿势上，而桌宠运行时是让它们受重力摆动的），所以形象一直不对。现在直接用满区正立的那张图（`满区.png` 截图）：抠掉黑底、切成「头」和「围脖」两层，作为**两层骨架**驱动——头绕脖子摆动、围脖反向小角度跟随，走起来还会朝行进方向侧身。
- **款式收敛为两只**：只保留满区（默认）与**绿冻**（原「果冻」第 2 只，绿果冻＋樱桃）；其余 6 款果冻与其贴图一并删除。满区不再是「果冻的第 0 款」，`petVariant` 配置项随之移除。
- **左右不对称，所以会翻面**：满区这张画左右并不对称，因此**向右走时整体镜像、走到最右端翻回原样**（镜像做在宠物图上，不作用在外层容器上——外层的 `transform` 每帧由动画循环写入，行内样式会盖掉样式表里的规则）。
- **两层改成硬切，消掉叠影**：上一版把「整张图」当头部层、再把围脖叠在上面，头一动，头层里那片围脖也跟着动，于是在围脖边缘露出一圈重复的描边。现在头部层是**原图减去围脖**（减去前把围脖区域向内收 3px，留一小片藏在围脖层下面，保证关节处不会开缝），两层不重不漏；围脖层还向上扩了 3px 把上沿的墨线一并纳入，避免那条描边留在头层里跟着跑。
- **文案**：满区的介绍改为「满区 · 一几一几，好可爱的一条区」。
- **选择卡的图框**：原来用 `display:grid` + 图片 `height:100%`，在网格里百分比高度会按内容撑开的行来解析，满区这张偏高的图于是顶出了框外。改为 flex 居中 + `overflow:hidden`，并统一成 52px 方框。
- 小宠物走行条的贴图（`manqu-walk.png`）与 `steps()` 逐格动画随旧图一起删除，宠物现在没有 8 帧循环，改为两层骨骼的连续摆动。
- **宠物栏紧贴内容**：内容底部到宠物头顶原本留着 28px（`#content` 自身的 20px 下内边距 + `#app` 的 144px），滚到底时最后一个卡片会浮在宠物头上方几毫米。现在 `#app` 的预留改为 116px，两者正好相接（实测 0px），宠物也仍然贴着底部导航栏（2px）。
- **「打开直播间」改为两段式**：原来点一下直接打开"第一位正在直播的主播"（没有直播时退化为第一位启用中的主播），无法选择。现在第一段展开一个内联面板：正在直播的排在最前（带红点、主播名与直播标题），未开播的排在后面；点其中一行才打开对应直播间，选完自动收起，按钮上的箭头会跟着翻转。面板列表自身限高 216px 内部滚动（滚动不穿透、iOS 式惯性滚动），主播再多也不会把页面撑长；`aria-expanded`／`aria-controls` 已标注。
- 版本号：`versionName 1.1.7`、`versionCode 39`（高于对外 1.1.6 的 35、内部 1.1.9 的 33，以及本轮 1.1.7 早先构建的 36–38，可直接覆盖升级）。
- 这次改动**没有碰「开播播报／响铃」这条链路**：`LiveGate`、`Prefs.armedAt()`、响铃与通知、时段语义、闹钟编号均未改动，核心断言（含 13 条开播判定契约）原样通过。

> 下方 1.1.6–1.1.9 各条是内部开发版的历史记录（内部版号与对外版号不同，对外 1.1.6 即"1.1.6（过渡版本）"那一条）。

## 1.1.6（过渡版本）

**这是一版「过渡版本」**：功能与 1.1.5 齐平，重点是检测可靠性与提醒时效的改进（见下）。「桌面小宠物」暂未包含，待美术素材授权确认后会随后续版本提供。

- 内容 = 1.1.5 的全部功能 + 以下可靠性改进：
  - **起始基线缺失时按「一直守候中」处理**：在守候早已开启、又跨版本升级等情形下，原实现会把「开始守候的时间」当作当前时刻，使该场直播被判成「开启守候时已在播」而跳过；在「补报已开播」关闭（默认）的情形下可能整场都不提醒。现在偏差至多多提醒一次，不会漏提醒。
  - **单个主播异常不再影响其他主播**：原先一位主播的数据异常会让那一轮所有主播都不判定（只留一条「本次检查未能完成」）；现在逐位隔离，问题主播跳过并记录，其余照常判定与提醒。
  - 新增开播判定契约测试（13 条），把「何时该响、何时不该响」逐条固定下来。
- **「桌面小宠物」暂未包含在本版**：其美术素材取自桌宠《可爱满区2.0》，再分发授权仍在确认（内部开发版曾包含、未对外发布）；确认后会随后续版本提供。
- 版本号五处同步 1.1.9 → 1.1.6（versionCode 34，高于此前所有公开版本，可直接覆盖升级）。
- **新增「持续高频守候」（默认开启，可在「设置 → 后台守候」关闭）**：针对「提醒比开播晚了几分钟」这类迟报。原因有两层：① 时段外原本会放宽到 3 分钟一次；② 更主要的是手机休眠（Doze）时系统会忽略唤醒锁、并把普通唤醒闹钟推迟到约 9 分钟一次。现在开启后：一直按设定间隔检查（时段外不放慢）、唤醒锁在守候期间常驻、并把循环兜底唤醒改为**闹钟式唤醒（`setAlarmClock`）**——这是 Doze 唯一不会推迟的唤醒方式，兜底间隔也从 3 倍缩到约 1.5 倍（30 秒间隔 → 约 45 秒兜底）。代价是更耗电、状态栏会出现闹钟图标，且需要「精确闹钟」权限（未授予时自动退回较弱方式）。**（该条中「时段外不放慢」的说法已被 2026-09-15 的修订取代：时段之外现在完全不检测，见本节第一段。）**
- 本次改进与 1.1.6 首次发布内容合并为同一版本：versionName 仍为 **1.1.6**，versionCode 提升为 **35**（首次发布的 1.1.6 为 34），因此已安装首次发布版的设备可直接覆盖升级。

> 注：下方 1.1.6–1.1.9 各条描述的是**内部开发版本**（含小宠物与推送前的中间修复）；对外的 1.1.6 即本版。

## 1.1.9

针对「直播间开播了但没响」这类**真实漏报**的加固。起因是用户实测遇到一次漏响，排查后没有找到会让「正常情况必然漏响」的缺陷（判定链本身是有测试覆盖的），但沿路发现两处**可能导致不提醒**的实现细节，已一并改进。

- **「起始基线」缺失时不再当作「现在」**：`armedAt` 是「从什么时候开始守候」的基准，用来判断某场直播是不是在你开始守候之前就已开播。原实现在这个值缺失时回退成**当前时间**，于是该场直播会被判成「开启守候时已在播」，在「补报已开播」关闭（默认关闭）的情形下可能整场都不提醒，且只有记录里能看到原因。现在缺失时按「一直守候中」处理 —— 这种偏差最多多响一次，绝不会漏响。
- **单个主播处理失败不再拖垮整轮检查**：原先每轮检查里任何一个主播的状态读取出现异常，都会让整轮中断，结果是**那一次所有主播都不判定**（只有一条「本次检查未能完成」）。现在逐位隔离：出问题的那一位跳过并在记录里说明，其余主播照常判定与响铃。
- 新增 `tests/LiveGateTests.java`（13 条断言）把「何时该响、何时不该响」逐条固定下来，包括：新开播必响、同场次不重复、更晚的一场会再响、**起始基线未知时仍要响**、守候期间才开播默认不响（补报开关才响）、以及「检测落在时段之外时不会响」这条已知边界（见下）。
- 版本号五处同步 1.1.8 → 1.1.9（versionCode 33）。

### 排查结论：开播没响只可能是这几种情况

每一条都会在「记录」里留下对应条目（记录页展示全部条目，不做过滤）：

| 记录里会看到 | 含义 | 怎么办 |
| --- | --- | --- |
| **检测到 X 开播** | 应用认定该响铃了 | 若没听到声音，检查勿扰模式、闹钟音量、铃声是否可播放；记录里同时会有「系统未授予声音焦点 / 手机未能播放铃声」 |
| **X 开启守候时已在播** | 你在该场直播**已经开播之后**才开启守候（或才添加这位主播） | 打开「补报已开播」；否则按设计跳过这一场 |
| **X 开播了（响铃已关闭）** | 这位主播的「开播响铃」被关掉了（检测照常） | 在主播页打开铃铛 |
| **X 开播了 / 已有主播提醒响铃中** | 另一位主播正在响铃，默认「同一时间只响一次」，这一场不再补响 | 可开启「接续响铃」改为排队 |
| **X 本场未响铃 · 开播或检测时间不在所选时段内** | 开播时间或检测时刻不在提醒时段内 | 调整时段，或开启「补报已开播」 |
| **守候曾被中断 / 守候似乎已中断** | 那段时间应用没在运行（被系统结束） | 允许自启动、电池设为不限制、最近任务锁定，或用无障碍守候辅助 |
| **本次检测被推迟** | 手机休眠把检测推后了（服务仍在运行） | 属于系统省电行为；授予「闹钟和提醒」与电池不限制可缓解 |
| **任何记录都没有** | 那段时间应用完全没运行，连「被中断」都没来得及记 | 同上，并检查是否被「强行停止」 |

已知边界（1.1.9 未改，因为属于「时段」语义）：**检测时刻落在时段之外时不会响**，即使这场直播的**开播时间**在时段内。手机休眠可把一次检测推到时段结束之后，此时这一场会被跳过。若希望这种「刚好跨过时段末尾」的情况也提醒，请告知，这需要按开播时间而不是检测时刻来判断。

## 1.1.8

- **修复「守候曾被中断」被反复误报**：这条警告的门槛只看「本次检测比计划晚了几分钟」，而**迟到几分钟并不代表守候停了**。三种完全正常的情况都会触发它：
  - **手机休眠（夜里最主要的原因）**：系统在 Doze 下不会给任何应用更频繁地派发唤醒闹钟 —— 精确闹钟约 9 分钟一次、未授予「闹钟和提醒」时约 15 分钟一次。门槛定的 5 分钟**低于系统自己的下限**，于是夜里每一个循环都被判成「中断」。默认的「凌晨守候」正好落在手机最会休眠的时段。
  - **时段外的计划**：时段外本就每 3 分钟才检查一次、且预期手机会睡。这份「闲置计划」留在磁盘上，等跨进某个时段后再比对，算出来的「迟到」是几十分钟的假数字。**（时段外的检查已在 2026-09-15 取消，此条描述的是当时的行为。）**
  - **一轮检查自己报告自己**：判定发生在「本轮是否正在检测中」之前，而计划时间要到本轮结束才更新。1.1.4 起每个主播都可能重试（17 秒超时 ×2 + 1.2 秒），主播多、网络慢时一轮能超过 5 分钟，于是守候在**正忙着检测**的时候把自己记成中断。
- **现在的判定按证据分类**，不再只看迟到几分钟：
  - 进程在本该检测的时刻之后才被创建 → 真的被系统结束了，记「守候曾被中断」（这是唯一有资格这么说的情况）；
  - 进程一直活着、手机醒着却仍迟到 → 记「本次检测被推迟」，说明通常是休眠或省电策略，**不再升级为警告**；
  - 手机正在休眠 → 不记录（休眠本身就会同时推迟心跳与检测，这是设计上接受的代价）；
  - 时段外的闲置计划 → 不参与判定。**（自 2026-09-15 起这种计划已不会再产生。）**
- **首页告警改用服务的存活状态而非心跳新鲜度**：状态推送与服务在同一个进程里，`running` 不会过期；而心跳恰恰会被「让检测迟到的那次休眠」一起推迟。原来心跳超过 3 分钟就报「服务已中断」，所以只要手机睡过一觉再打开应用就会看到红卡。现在只有**服务真的没在运行**才报中断；服务活着但心跳偏久时，改为一句话说明「仍在运行，休眠会同时推迟心跳与检测」。
- 判定规则收敛为纯 Java 的 `PollPlan.classifyLate`，随核心测试一起跑（新增 11 条断言）。
- 版本号五处同步 1.1.7 → 1.1.8（versionCode 32）。

## 1.1.7

- **小宠物换上「满区」本体**：满区是「可爱满区」的桌宠主角（灰发、白毛领的那只），此前只做出了它的七种点心皮「果冻」，本体一直没取出来。现在「设置 → 桌面小宠物 → 选择款式」里**满区排在第一个并作为默认**，它会**自己走路**（8 帧循环），果冻的七种口味仍在后面可选。
- **补齐第 7 只果冻**：1.1.6 只放出 6 款，漏掉的**黄色笑脸款**（`果冻 (7)`）已补上，现在共 7 款。
- 满区是可直接切换的**角色**，与果冻的「款式」互不覆盖：切回满区时你会保留原先选的口味。
- 美术仍然只来自桌面版《可爱满区2.0》，**未新增任何来源**；提取与渲染管线已固化为 `tools/bake_pet_art.py`（详见该文件头部说明）。

### 关于满区是怎么取出来的

桌面版把角色存成 **Spine 4.2 骨骼**（63 根骨、37 个槽位，动画含 `idle`／`walk2` 等 10 条），而不是整张立绘。Android 跑不了 Windows x64 程序，装 Spine 运行时也不合适，所以改为**在这台机器上离线烘焙**成平铺 PNG，应用里只放图片。

骨骼数据有两处与常见写法相反，已按其自带图标逐个核对（若按常见写法渲染，角色会被撕成碎片）：

| 项目 | 常见写法 | 这套骨骼实际是 |
| --- | --- | --- |
| `rotate` 关键帧 | 绝对角度 | **相对静止角的增量**（骨骼静止在 158°，动画只写 2°） |
| `translate` 关键帧 | 绝对坐标 | **相对静止位置的偏移** |
| 尾巴骨骼 | 直接播动画 | 由 **Spine 物理**驱动，需按「保持静止世界朝向」求解 |

## 1.1.6

- **新增桌面小宠物**：应用底部多了一只会在屏幕下方来回爬的小宠物，走一会儿会自己跳一下。可在「设置 → 桌面小宠物」里开关与换款。它只在打开应用时活动、不联网、不占后台，也不影响守候与耗电。
  - 小宠物走在底部导航上方**单独留出的一栏**里，不会压住导航按钮。
  - 它被特意做成**点击穿透**的：固定在底部横扫的元素若可点击，就会吞掉本该落到卡片上的点击；在闹钟应用里漏一次点击比「能戳一下宠物」更糟。想逗它请用设置里的「逗它一下」。
  - 关掉后那一栏的留白会一起收回；开启「减弱动态效果」时它不再移动和跳动。
  - 美术素材取自同一角色的桌宠《可爱满区2.0》（Unity 桌面版）。

## 1.1.5

- **「周表」页排版重做**：此前一张卡片里塞了头像、主播名、「计划开播」状态、备注和时间四样东西，挤在很窄的一行里，状态文字被压成**一个字一行**（竖着排成「计/划/开/播」），卡片也被撑得很高。现在时间与直播缩略图独占右侧一列，主播名在上、状态与备注在下：时间右对齐、等宽数字、不再换行；备注过长自动省略号（完整内容仍可在主播的周表编辑器里看到）；头像与卡片内边距收紧，一屏能看更多场。窄屏（≤360dp）另有适配。空状态也改成图标 + 标题 + 引导按钮的样式。

> **本版同时包含 1.1.4 的全部修复**（1.1.4 的安装包已被本版取代，直接装 1.1.5 即可）：
> - 添加主播改以 **UID** 为主，并修复「未开通直播间」的误报（接口字段读错位置）
> - 修复换背景后仍然显示第一张图（固定地址被缓存）
> - 选图片不再「假成功」（导入完成才回复，失败会说原因）
> - 「外观与个性化」并入「主题与外观」，主题色圆点显示实际生效色
> - 修复「按周表预告提醒」把开播时间说晚 5 分钟

## 1.1.4

- **添加主播改以 UID 为主，并修复「未开通直播间」的误报**：之前输入 UID 会被判成「这个账号还没有开通直播间」，其实是接口字段读错了位置——B 站把直播间号放在 `data.room_id`，而 `data.info` 里只有昵称头像、根本没有这个字段，于是每个 UID 都被当成没开播。现在裸数字按 **UID** 解析（主播主页 `space.bilibili.com/` 后面那串数字，如 `1298779265`），输入框标签、示例与说明都据此改写；直播间链接仍照旧可用。
- **修复换背景后仍然显示第一张图**：应用请求的背景地址是固定别名 `/background/current`，地址不变时浏览器/WebView 会一直用缓存里的第一张图片，所以选第二张、第三张都看不出变化。现在地址会带上随图片变化的版本号，换图立即生效。
- **选图片不再「假成功」**：点「选择图片」后如果导入失败或取消，以前界面会先提示已保存，让人以为功能坏了。现在导入真正完成才回复结果，失败会说明原因。
- **「外观与个性化」并入「主题与外观」**：原先「界面主题」与「外观与个性化」分成两处，而 AMOLED 只能在夜色主题生效，并列展示容易被读成互相冲突。现在明暗、AMOLED 与主题色在同一张卡片里，各自写明生效条件。主题色圆点也改为显示**当前主题下实际生效的颜色**（夜色下会提亮），点下去所见即所得。
- **修复「按周表预告提醒」把开播时间说晚 5 分钟**：这个功能本身是正常工作的（按周表开播前 5 分钟发一条不响铃的通知），但通知里写的时间比周表实际写的晚了 5 分钟——例如周表是 20:00，通知却说 20:05。原因是「提前量」在存和取时被减了又加、重复计算了一次。现在通知与桌面小组件显示的时间与周表完全一致；提前量收敛为一个常量（`PollPlan.PRESTREAM_LEAD_MILLIS`），避免再次算错。另外补上一处保护：没有后续安排时不会再排一个「立即触发」的空闹钟。

## 1.1.3

- **修复外观与界面主题的冲突**：主题色与「卡片不透明度」此前写在 `<html>` 上，会被深色主题的 `body.dark` 样式覆盖——表现为**深色模式下主题色不生效、卡片不透明度也不生效（背景图被纯色卡片挡住）**。现在两者在明暗主题下都生效。另：选择「跟随系统」时，系统切换深浅色会立即跟随。
- **添加主播更方便**：输入框不再只认号码——可直接粘贴直播间链接（live.bilibili.com/1713546334）、主播主页链接（space.bilibili.com/1298779265）或「UID 1234567」，应用会自己解析；粘贴主页链接时先由 UID 查出对应直播间再识别名称与头像。
- 提示文案与输入键盘同步调整（原来的纯数字键盘无法输入链接）。

## 1.1.1

- **网络失败更可解释、更少发生**：你截图里那种「Socket failed: Connection reset by peer」是 B 站服务器或中间网络主动断开连接（接口风控、网络切换、信号抖动），与手机权限无关。现在：①每轮检测中主播请求之间加 400ms 间隔，降低被服务器掐断的概率；②单个主播的瞬时断连会在 1.2 秒后自动快速重试一次，多数情况下这一轮直接成功，不再留下失败记录；③兜底失败时的记录文案改为可读的中文解释（如「B 站服务器中断了连接（接口风控或网络抖动，不是权限问题）」）。
- 唤醒锁预算按「每个主播两次请求」计算，覆盖重试耗时。
- 小组件标题同步更名为「VR闹钟」。
- 修复自选背景图片完全不生效：界面点击「选择图片」「移除背景」在脚本里没有接到动作（点了没反应），且界面请求的图片地址与应用能识别的地址不一致（恒定 404）。现在点击可正常导入/移除，背景即时显示并在设置页可见。
- AI 周表识别升级：提示词更具体并告知当天日期，输出增加「日期」列；识别到具体日期（2月15日、2/15）或相对日期（今天/明天/后天）时自动换算成对应星期，该安排只落在那一天。周表编辑器新增「清空全部」按钮，过完周末可一键清除该主播的安排。

## 1.1.0（试运行版）

- 本仓库试运行版，并入内部迭代 1.1.0–1.4.3 的全部能力：多主播守候与接续响铃、周表（手动/文字识别/AI 识别/图片对照）、开播统计、桌面小组件、外观自定义（主题色/AMOLED/自选背景）、烘焙圆角的满幅位图图标、定时恢复检查等；完整变更明细见下方内部版本记录。
- 应用不再内置 AI 接口密钥：需在「设置 → AI 图片识别」自行填写 DeepSeek 兼容密钥。
- 应用显示名更名为「VR闹钟」（包名不变；下方历史条目中的「主播闹钟」为曾用名）。
- 仓库已清理私人数据与签名材料路径；上游 release 产物不入库。


## 1.4.3

- **开源准备**：设置里的 AI 接口密钥不再有内置默认值——应用出厂为空，需在「设置 → AI 图片识别」自行填入你自己的 DeepSeek 兼容密钥（仅保存在本机，导出备份不含密钥）。此前从旧版升级的设备不受影响，重装后需重新填一次。
- 移除了测试工具与内部文档里的私人数据（真实主播 UID/房间号、签名材料路径）。

## 1.4.2

- **图标再修**：构图恢复到与原版一致——人物向下裁到小腿（有部分腿），不再只有上半身；圆角不再依赖桌面蒙版，直接烘焙进图片的透明圆角里，任何桌面都显示为无棱角圆角框。
- **移除「从置顶动态获取」**：B 站接口风控导致该功能成功率没有保障，按反馈整体下线（按钮、动态接口与 Wbi 签名代码一并移除）。周表仍支持：手动逐条添加、粘贴文字识别、上传图片对照、AI 识别。
- 修复设置页「数据与帮助」列表排版：行右侧箭头错误地参与了弹性布局，把文字挤到折行；现在每行单行显示、箭头靠右。

## 1.4.1

- **图标改为满幅位图**（接 1.3.6）：荣耀等定制桌面会把自适应图标压平到白色底板上显示（直角、人物缩小），且缓存严重。现改为与旧版满区闹钟一致的做法——渐变底 + 上半身喇叭立绘直接烘焙成全出血方形位图，全密度提供（48–192px），由桌面自行裁圆角。安装后若图标未刷新，请**先卸载、重启手机、再安装**一次以清空桌面图标缓存。

## 1.4.0

- 移植原版满区闹钟 1.0.6 的四项设计（Webview 架构下的等价实现；原版的 Compose 原生界面本身不适用）：
- **自选背景图片**：设置 → 背景图片，选一张喜欢的图作应用背景（≤20 MB，自动缩放，仅保存在应用内）；「背景遮罩」压暗背景保证文字可读，「卡片不透明度」控制透出程度。响铃页保持原有设计不显示背景。
- **主题色**：预设 12 色色板 + 自定义 HEX，明暗两套按种子色自动推导（与原版同一公式）；**AMOLED 纯黑模式**：深色主题下使用纯黑底色。
- **从最近任务隐藏缩略图**（默认关）：隐藏最近任务卡片里的应用内容，减少误划与窥屏；不影响提醒。
- **定时恢复检查**（默认开）：每约 15 分钟用「经过时间」闹钟（不受系统时间跳变影响）确认守候服务是否还活着，中断时自动拉起并写入记录——与原有的保活、看门狗、无障碍辅助互为第四层保险。设置页显示下次恢复检查时间。
- 备份仍不包含背景图片与自选铃声；导出继续剥离 AI 密钥。

## 1.3.7

- **主播卡片排版修复**：UID/直播间号与底部统计按整段折行，「直播间」「1 场」不再被劈成两半（如「直/播间」「本周 1/场」）；统计文字移到分隔线正下方独立一行，「周表/编辑/直播间/空间」按钮单独一行，垂直对齐统一；「编辑」图标由箭头改为铅笔，与周表（日历）、直播间/空间（外链）的语义图标风格一致。

## 1.3.6

- **修复桌面图标**：此前图标前景直接复用 3.5 MB 全尺寸立绘，部分桌面（荣耀等）解码困难时会退化成「白底直角方块、人物小」的未加工观感。现按旧版满区闹钟的样子重新加工：裁取上半身放大（喇叭贴嘴、齐裙截断）、抠除白底、背景改为浅灰白渐变，前景降为 120 KB 独立资源；APK 因此瘦身约 3.5 MB。若安装后桌面图标未立刻刷新，重启一次桌面（或卸载重装）即可。
- 说明：公开仓库与 1.0.5 公开包里从来只有未加工的原始立绘，旧桌面上的成品图标来自作者本地构建，无法直接还原，本版为按观感重制。
- 响铃页与周表使用的 `assets/hazel.png` 插画不受影响。

## 1.3.5

- 响铃页恢复使用原先的 hazel 插画：1.3.2 起封面图会在响铃时替换插画，直播间截图在这一页观感差；封面缩略图保留在周表时间线上。
- 新增**桌面小组件**：守候状态、正在直播的主播、下一次周表预告与上次检测时间，一目了然；每轮检测与心跳都会刷新，纯本机数据。
- 发布前自查修复七处：预告闹钟的时间计算从「只看今明两天」改为「扫整周」（周六本来看不到下周五的场次）；预告通知与小组件显示的是开播时刻而非提醒时刻（差 5 分钟）；导出备份剥离 AI 密钥（避免误分享）；AI 识别 max_tokens 提高到 4000（推理模型可能耗尽配额导致空回复）；封面代理非 200 时断开连接；重启后小组件立即刷新一次。
- 注意：早先打包的 1.3.1–1.3.4 安装包含上述小问题，已被本版取代，请直接安装 1.3.5。

## 1.3.4

- 新增**多主播接续响铃**（声音页开关，默认关）：开启后，第一位响铃期间开播的其他主播按顺序接续提醒，而不是只记录；手动关闭响铃会跳过整条队列。
## 1.3.3

- 新增**开播统计**：按主播记录每场开播与结束（本机保留最近 60 场），主播卡片显示「本周 N 场 · 近 30 天 N 场」。
## 1.3.2

- 新增**直播间封面**：正在直播的主播在周表时间线与响铃页显示直播间封面（检测接口本就返回，无新增请求；封面经应用自身代理加载，仅限 B 站图床域名，界面仍不直连外网）。
## 1.3.1

- 新增**即将开播自动提速检测**：周表显示某主播 30 分钟内要开播（含开播后 30 分钟）时，即使处于提醒时段外，检测间隔也从约 3 分钟的低频自动提回你设置的间隔，过后自动恢复省电；守候通知会显示「临近周表开播 · 高频检查中」。**（已被 2026-09-15 的修订取代：时段之外不再检测，也就不存在「时段外提速」；时段内本来就按设定间隔检测。）**
- 新增**按周表预告提醒**（时段页开关，默认开）：周表开播前 5 分钟发一条「快开播了」提醒通知，不响铃；仅对已开启响铃的主播生效。

## 1.3.0

- 新增 **AI 识别周表图片**：上传周表图片后，在周表编辑弹窗点「AI 识别图片安排」，AI 自动提取“星期|开始|结束|备注”并进入确认列表，确认后才保存——与文字识别共用同一套人工把关。
- 已用 DeepSeek `deepseek-flash`（视觉）真接口端到端验证：中文周表截图 → 结构化安排行，无时间的行（如“休息”）正确跳过，单张约 577 tokens。
- 设置页新增「AI 图片识别」区块：开关（默认开）、接口密钥与模型名（需支持图片输入）。密钥仅保存在本机。
- **隐私变化（默认开启，请知悉）**：点按 AI 识别时，该张周表图片会经压缩后上传到你配置的 AI 服务商处理；不点按不上传。可随时在设置中关闭。
- 超过 1.5 MB 的图片先缩放到约 1280 px 再上传（识别文字足够，流量更小）。
- **内置的默认接口密钥仅供个人使用：若把本仓库公开或推送，请先在设置中更换密钥并从 `Prefs.java` 的默认值中移除。**
- 识别失败会显示服务商返回的具体原因；流程失败不写任何数据，不影响手动录入。

## 1.2.2

- 新增「刷新全部头像」：主播页一键为所有主播重新获取头像。此前头像只在识别时下载一次，当时失败（网络抖动或限流）就永远是首字母占位——这正是“部分主播没有头像”的原因。
- 置顶动态抓取重写：B 站已下线旧版免登录动态接口（HTTP 404 的直接原因），改为带 Wbi 签名的现行动态接口（导航接口取密钥 + 访客凭证），并保留粘贴动态链接的读取。**新接口仍受 B 站风控影响，失败时照旧指向手动粘贴或上传图片。**
- 周表图片改为「对照模式」：上传的图片直接显示在编辑列表上方，边看图边逐条录入。

## 1.2.1

- 新增「从置顶动态获取」：在周表编辑弹窗里一键读取该主播置顶动态的文字，填入来源框后点「识别为安排」进入确认流程；也支持直接粘贴 `t.bilibili.com/<动态id>` 链接抓取单条动态。
- 实现走免登录的旧版动态接口并做了多层数据形态兼容；**接口随时可能被 B 站风控拦下（-352 等）**，失败时会明确提示，转手动粘贴或上传图片，不影响任何已有功能。
- 读取置顶动态会让 B 站服务收到常规请求信息，已同步写入隐私说明。

## 1.2.0

### 响铃独立开关

- 每位主播从单个开关变为两个：「检测」决定是否检测（关掉即完全不看）；「响铃」决定检测到开播时是否响铃——关闭后照常检测、时间线照常亮起，只在记录里写一条「响铃已关闭」。
- 响铃关闭的场次被标记为已处理：中途重新打开响铃，不会为已经播了一半的场次突然补响，下一场才生效；也不会占用「同一时间只响一次」的坑位。
- 新增主播弹窗里也有「开播响铃」开关（默认开）；备份恢复、旧版本数据迁移均兼容（旧数据默认响铃开）。
- 自检发现并修复：仅切换「检测」开关时响铃开关会被重置回开启；响铃状态现在在缺省时保留原值。

### 周表与时间线

- 新增「周表」页（导航第 6 个标签）：按周一分组展示各主播的直播安排，今天在最前并标注，正在直播的主播亮绿色圆点，跨午夜结束时间按次日理解。
- 每位主播最多 16 条安排（星期位掩码 + 开始时间 + 可选结束时间 + 备注）；数据模型为纯 Java 校验（20 条断言）+ 容错读取，坏条目丢弃不致命。
- 编辑弹窗三种来源任选：逐条添加（星期选择器 + 时间 + 备注）、粘贴周表文字自动识别（宽松匹配「周X/星期X/礼拜X/周末」+「20:00/20点/下午2点」等写法，识别结果进列表人工确认后才保存）、上传周表图片（仅展示，明确不做 OCR）。
- 周表原文保留在本机，重新打开编辑器无需重新抓取；周表只用于展示，是否响铃仍由「时段」页与响铃开关决定，两套时间规则互不干扰。
- 更换房间号会清空该主播的周表（周表属于直播间）；删除主播时周表、原文与图片一并清除。
- 自检发现并修复：周表编辑弹窗重绘会清空正在输入的来源文字；重绘现在保留当前输入。

## 1.1.2

- 修复：**主播头像从来没有显示过**。界面用相对路径请求 `avatar/<id>.png`，实际落在 `/assets/avatar/…`，而应用自身的头像路由只认 `/avatar/…`，请求被打到不存在的打包资源上返回 403，界面只好退回首字母占位。已改为绝对路径；界面测试的服务器现在提供真实头像文件，头像能否渲染从此被场景真正覆盖。
- 新增：编辑主播弹窗里的「重新下载头像」按钮，头像下载失败或主播换过头像时无需删除重加。
- 新增：没有头像时的首字母占位改为按主播编号稳定取色的渐变底色，不再是纯灰块。

## 1.1.1

真机反馈：从最近任务划掉应用后，守候仍不恢复。复查后确认这是系统的强杀行为，不是可以靠应用代码否决的操作（这一限制对已发布的 1.0.5 同样成立，且 1.0.5 连恢复用的闹钟都没有）。本版做两件能做的事：把中断如实记下来，并把真正管用的设置讲清楚。

- 新增中断记录：检测实际开始时间比服务自己安排的时间晚 5 分钟以上、且当时处于提醒时段内时，写入一条「守候曾被中断」记录，附上晚了多久与要检查的设置。这是区分「恢复用的闹钟根本没被投递」和「闹钟投递了但系统拒绝启动服务」的唯一证据——两者症状相同，修法完全不同。
- 守候页的中断提示补充了最容易被漏掉的一步：在最近任务里长按本应用卡片将其**锁定**。从最近任务划掉应用时，系统会连同恢复用的闹钟一起停掉，只配自启动与电池不限制仍然不够。
- 守候页的中断提示新增「开启无障碍守候辅助」入口（未启用时显示）。无障碍服务由系统自己重新绑定，因此系统清理本应用后仍会把它拉起来，再由它恢复守候——这是划掉后台后恢复最快的一条路；说明文案也写清了这一点与前提条件。
- 新增：荣耀 MagicOS 的设置路径写进设置页与使用说明（应用启动管理 → 关闭自动管理 → 勾选自启动、关联启动、后台活动）。
- 修复：系统反复拒绝后台启动时，「守候未能启动」记录会按保活闹钟的节奏（约 90 秒一条）刷满记录页；现在同类失败最多每 10 分钟记一条。
- 修复：前台服务未能进入前台时，保活闹钟不再被遗留下来（现在只在服务成功进入前台后才安排）。

验证：40 个界面与模拟通信场景通过（中断提示场景扩展为同时校验最近任务指引、无障碍入口与说明文案）；核心规则断言 36038 + 12 + 13 + 46 + 72 条通过；24 个源文件括号配平通过。APK 已用 Android SDK 35 重新编译、D8、zipalign 并通过 apksigner v2/v3 校验，`versionCode=8`、`versionName=1.1.1`。**仍未在真机验证。**

## 1.1.0

- 新增“主播”页：填入 B 站直播间号码即可自动识别主播名称与头像，最多 16 位主播，可增删、改名、分别开关。
- 应用显示名改为「主播闹钟」（启动器名称、界面标题、通知标题与无障碍服务名一并统一）；仓库与文档中的项目名仍为满区闹钟。
- 守候改为按主播逐一检测：每位主播各自记录场次去重状态，同一时间只响一次铃，其余主播开播时写入记录而不叠加响铃。
- 提醒时段与铃声由所有主播共用；每位主播只有独立的启用开关。
- 头像在识别时下载一次并缓存到应用私有目录，由应用自身提供，界面不向外部网络直接请求图片；识别失败时退回首字母占位。
- 轮询随主播数量调整：唤醒锁覆盖整轮检测，单轮耗时计入间隔，避免主播变多后检测明显滞后。
- 限流退避按整轮判断：任一主播触发 B 站限流即放慢整轮重试，并把失败主播计为一次失败，不再逐主播静默重试。
- 备份与恢复包含主播列表；删除主播时一并清除该主播的场次状态与缓存头像。
- 升级时自动沿用原有单主播的场次状态，不会在升级后重复提醒同一场直播。

### 全天守候的可靠性

- 新增检测保活闹钟：每轮检测结束后，按本轮间隔的三倍安排一次系统唤醒（下限 90 秒）。检测循环跑在主线程消息队列上，本身没有唤醒能力，设备休眠或厂商后台限制会让它悄悄停住；保活闹钟是唯一能在用户不开应用的情况下把它重新拉起来的东西。循环正常时每轮都会把它推后，因此它只在真的停住时才触发。
- 新增守候看门狗：每 15 分钟由系统闹钟检查一次服务心跳，服务已停就在后台尝试自行恢复。此前全天守候不安排任何定时唤醒（时段边界只对自定义时段生效），进程被系统杀掉后只能靠重启手机或手动打开应用才会恢复——这是“整天没有提醒”的直接原因。
- 新增异常兜底：单次检测中的内部错误不再让整条轮询静默终止，也不再以未捕获异常结束进程；错误记入记录后仍按间隔重试。
- 新增：守候页在服务心跳超过 3 分钟未更新时明确提示“守候服务已中断”，并给出重新开启与自启动、电池不限制的设置指引。
- 修复：检测成功时没有清除“网络未连接，联网后会自动重试”提示，一次网络波动后该提示会一直留在守候页。现在与 1.0.5 一致，成功检测即撤下提示。
- 修复：Wi-Fi 与移动数据切换的瞬间可能被误判为断网；现在只要存在任一可用网络就不再算断网，避免误报并避免无谓地放慢重试。
- 修复：后台启动被系统拦下后留下的提示会一直挂在守候页；现在启动类失败单独记录，服务起来或检测成功即撤下，不再与“通知被拒”提示混用同一个位置。

### 正确性

- 修复：新增主播、或给已有主播换房间号时，会沿用该编号上一次的场次去重状态；现在这类改动会重置该主播的场次与上次状态，并把它的“加入时间”记为当前时刻——加入时已经在播的直播不会再被当成刚开播而误响。
- 修复：与界面文案不一致的互斥响铃。此前第二位主播开播只写一条记录并不响铃，但那一场并没有被标记为已提醒，于是第一位主播的响铃一停，下一次检测就会接着响——正是文案所承诺不会出现的“叠着响”。现在被跳过的场次会标记为已处理，只记入记录。
- 新增：真实开播可以打断正在进行的响铃测试（此前测试响铃期间检测到的开播会被当作重复而丢弃）。
- 修复：直播间身份校验同时比对了房间号与主播 UID，而 B 站两个接口对同一房间可能给出不同的 UID，一旦不一致该主播就永久无法提醒。现在只按房间号校验，UID 仅用于跳转个人空间。
- 修复：填入不存在的房间号时提示“B 站接口返回 -400，等待重试”，现在提示“没有找到这个直播间，请检查房间号”。
- 修复：发布版号在多个文件里写死，改一次版本要改多处且容易漏。界面页脚与通知排查包改为从已安装的包信息读取真实版本号，不再有写死的版本常量。

验证：40 个界面与模拟通信场景通过（含 8 个主播页与守候状态场景）；核心规则断言 36038 + 12 + 13 + 46 + 72 条通过；24 个 Android 源文件括号配平通过。Android 端 Java 已用 Android SDK 35 与 build-tools 35.0.0 完整编译，经 D8、zipalign 并通过 apksigner v2/v3 签名校验，产物为独立包名 `dev.hazel.livealarm.multi`（应用名「主播闹钟」，可与原版并存），`versionCode=7`、`versionName=1.1.0`。**尚未在真机运行**，范围见 [测试记录](docs/TESTING.md)。

## 1.0.5

- 守候通知固定为前台服务通知，响铃使用另一条通知，结束响铃后继续保留守候状态。
- 通知提供立即检测、停止守候等操作，响铃通知提供停止与稍后提醒操作。
- 授权状态发生变化或回到应用时重新读取权限与通道状态，并更新界面。
- 设置页分别显示服务运行、系统授权、通道状态和通知登记情况，避免把服务运行误写成通知已显示。
- 停止守候直接撤销检测和相关定时任务；不再为了执行停止动作而新建服务。
- 前台服务临时启动失败时保留先前的守候设置；开启守候时的测试结束后继续正常检测。
- 保留 1.0.3 的显式同意兼容响铃，以及之前的铃声选择和设置即时刷新修复。

验证：30 个界面与模拟通信场景通过；13 个通知状态与 12 个响铃策略断言通过。原生测试 APK 已编译，尚未由本项目验证环境在真机执行；完整范围见 [测试记录](docs/TESTING.md)。

通知开关被设备管理策略固定拒绝时，需调整对应管理策略。本版识别并解释该状态，不宣称可以绕过它。

## 1.0.4

- 识别系统策略拒绝通知的状态，避免重复请求已被策略阻止的运行时授权。
- 增加用户主动导出的通知诊断与可选系统权限组件导出。

## 1.0.3

- 应用名称统一为“满区闹钟”。
- 新增“通知异常时仍响铃”，须由用户明确开启。
- 增加响铃时可选的悬浮停止入口，普通使用不依赖外部授权工具。

## 1.0.1–1.0.2

- 修复设置保存和异步状态刷新竞态：铃声、开关、主题等选择无需切页即可显示。
- 修复只有权限变化时界面不更新的问题，增强返回应用后的授权读取和系统设置入口。
