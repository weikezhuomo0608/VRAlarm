> 本仓库公开版本号从 1.1.0 起（=内部版本 1.4.3，versionCode 25）；下文出现的 1.1.x–1.4.x 为开发期内部版本号。

# 验证范围

## 1.1.4（UID 添加主播 · 背景切换 · 主题设置合并 · 周表预告时间修正）

| 验证 | 结果 | 范围 |
| --- | --- | --- |
| 界面回归 | 58 个场景通过 | 在 1.1.3 基础上新增 5 个：裸数字按 UID 解析并在保存时保留输入、换背景后图片地址随版本号变化、主题卡不再被拆成两处、主题色圆点随明暗主题显示实际生效色、时段页的周表预告开关文案与持久化；另修正 4 个原有场景（裸数字语义变化、失败查找改用直播间链接） |
| 核心规则断言 | 36,038 + 12 + 13 + 51 + 85 + 20 条通过 | 新增 4 条：提前量常量为 5 分钟、闹钟恰好早一个提前量、存储的开始时间可由「闹钟时刻+提前量」精确还原、开始时间已过则当天不再排闹钟 |
| 周表预告时间自检 | 通过 | 独立程序复算：周表 20:00 → 闹钟 19:55 → 通知/小组件显示 20:00（修复前显示 20:05）；无后续安排时传入调度器的值为 0，不会排出立即触发的空闹钟 |
| Java 结构自检 | 30 个文件配平通过 | — |
| 构建脚本自检 | 通过 | `build_apk.py` 兼容 Windows 路径与 `aapt2.exe`/`zipalign.exe` 后缀 |
| B 站接口 | 真实接口核对 | `live_user/v1/Master/info` 的直播间号在 `data.room_id`，`data.info` 无此字段；修复前后差异已确认 |
| APK 编译与签名 | 通过 | `build/VRAlarm-1.1.4.apk`，`versionCode=28`、`versionName=1.1.4`，apksigner v2/v3 校验通过；发布附件的 SHA-256 与本地构建一致 |
| 目视检查 | 通过 | 设置页「主题与外观」、暗色首页、添加主播弹窗截图 |

**尚未真机验证**：换背景在真实相册选择器下的重复切换、UID 添加在真实网络（含风控限流）下的成功率、主题色在真机深色模式下的观感、周表预告通知在真机（尤其荣耀 MagicOS）的送达与免打扰表现。

## 1.3.5（提速检测 · 预告提醒 · 封面 · 统计 · 接续响铃 · 小组件）

| 验证 | 结果 | 范围 |
| --- | --- | --- |
| 界面回归 | 49 个场景通过 | 含统计展示 |
| 核心规则断言 | 36,038 + 12 + 13 + 51 + 90 + 20 条通过 | 新增 18 条：临近开播判定（前 30 分钟/后 30 分钟/错日/空表）与预告时间计算（提前量、跳过今日、滚动下周） |
| Java 结构自检 | 28 个文件配平通过 | 含新增 WatchWidget.java |
| APK 编译与签名 | 通过 | 1.3.1–1.3.5（versionCode 14–18） |

**尚未真机验证**：提速检测在真实休眠下的实际提频效果、预告通知在荣耀的送达、封面代理在弱网的表现、小组件刷新节奏。

## 1.3.0（AI 识别周表图片）

| 验证 | 结果 | 范围 |
| --- | --- | --- |
| 真实接口端到端 | 通过 | DeepSeek `deepseek-flash` 视觉：中文周表截图 → 结构化安排行（4\|20:00\|22:30\|游戏回 等），无时间行正确跳过，577 tokens |
| 界面回归 | 49 个场景通过 | 新增 AI 识别场景：图片进入确认列表、确认前不落盘 |
| 既有回归 | 36,038 + 12 + 13 + 51 + 72 + 20 条通过 | 未受影响 |
| APK 编译与签名 | 通过 | `build/ManquAlarm-multi-1.3.0.apk`（versionCode 13） |

**尚未真机验证**：真实手写/截图周表的识别率（AI 识别率因图而异，保存前的人工确认是兜底）、大图压缩路径、密钥错误与欠费时的报错展示。

## 1.2.2

| 验证 | 结果 | 范围 |
| --- | --- | --- |
| 界面回归 | 48 个场景通过 | 新增批量头像刷新场景 |
| 既有回归 | 36,038 + 12 + 13 + 51 + 72 + 20 条通过 | 未受影响 |
| APK 编译与签名 | 通过 | `build/ManquAlarm-multi-1.2.2.apk`（versionCode 12） |

- 头像缺失的根因确认：头像只在识别时下载一次，失败后无补抓路径；新增「刷新全部头像」逐位补抓（每主播 1 次接口请求 + 1 次图片下载，间隔 800 ms 防限流）。
- 置顶动态改走 Wbi 签名接口（nav 密钥 + finger/spi 访客凭证 + feed/space），旧接口已 404。**新接口在真机的实际成功率未验证**，失败仍明确指向手动方式。

## 1.2.1（周表 · 响铃开关 · 置顶动态）

| 验证 | 结果 | 范围 |
| --- | --- | --- |
| 界面回归 | 47 个场景通过 | 在 1.1.1 基础上新增 7 个：响铃开关独立于检测、周表页按天分组与直播状态、粘贴文字识别后确认保存、逐条添加走星期选择器、周表图片上传/查看/删除、动态链接抓取进确认流程、置顶动态按钮填充来源框 |
| 主播列表规则 | 51 个断言通过 | alarm 标志默认值与翻转 |
| 周表规则 | 20 个断言通过 | 每主播上限 16、编号字符集、星期/时间范围、结束时间缺省（-1）、备注截断、容错修复、星期位匹配 |
| 既有回归 | 36,038 + 12 + 13 + 72 条通过 | 时间与场次去重、响铃策略、守候通知状态、轮询与保活计时 |
| Java 结构自检 | 26 个文件括号配平通过 | 含新增 Schedule.java |
| APK 编译与签名 | 通过 | `build/ManquAlarm-multi-1.2.1.apk`（versionCode 11），apksigner v2/v3 |

自检发现并已修复的问题：①仅切换「检测」开关会把响铃状态重置回开启（桥接载荷缺省时 Java 现在保留原值，UI 侧同时总是携带该字段）；②真机上 `/schedule/` 图片路由缺失（界面测试服务器此前代为应答，掩盖了拦截器缺路由）；③周表编辑弹窗重绘清空正在输入的来源文字；④头像路由自 1.1.2 起改为绝对路径并被场景真正覆盖。

**尚未真机验证**：周表识别对真实置顶动态文字的命中情况（B 站接口可能被风控拦截，属预期内的失败路径）、双开关在响铃互斥中的实际表现、周表图片上传（Android 存储框架路径）。

## 1.1.1

| 验证 | 结果 | 范围 |
| --- | --- | --- |
| 界面回归 | 40 个场景通过 | 中断提示场景扩展为同时校验「最近任务锁定」指引、无障碍辅助入口（未启用时出现）与其说明文案 |
| 核心规则断言 | 36038 + 12 + 13 + 46 + 72 条通过 | 时间与场次去重、响铃策略、守候通知状态、主播列表、轮询与保活计时 |
| Java 结构自检 | 24 个文件括号与引号配平通过 | `tools/check_java_balance.py`；不是编译器 |
| APK 编译与签名 | 通过 | `build/ManquAlarm-multi-1.1.1.apk`，`versionCode=8`、`versionName=1.1.1`，apksigner v2/v3 校验通过 |

**第三次真机反馈（2026-09-11）**：从最近任务划掉应用后，守候仍不恢复。

- 这是**系统行为，不是缺陷**：在小米/澎湃等系统上，划掉应用等于系统级强杀——前台服务、已授予的权限、以及恢复用的闹钟会被一起停掉，应用无权否决。已发布的 1.0.5 同样如此，而且它连恢复用的闹钟都没有（全天守候无任何定时唤醒），所以「原版能做到」并不成立。
- 与本次反馈同时成立的一个事实值得记录：1.1.0 已埋了约 90 秒的保活闹钟与 15 分钟看门狗，划掉后仍不恢复，说明恢复用的闹钟很可能根本没有被投递（自启动未生效），或投递了但系统拒绝启动前台服务。
- 本版据此新增两件事：①**中断记录**——检测实际开始时间比服务自行安排的时间晚 5 分钟以上、且当时处于提醒时段内时，写入「守候曾被中断」并附上晚了多久；这是区分上述两种情况的唯一证据。②**指引补全**——守候页的中断提示补上最容易漏掉的一步（最近任务里长按卡片锁定）与「开启无障碍守候辅助」入口，无障碍说明写清它为何是划掉后台后恢复最快的一条路（无障碍服务由系统自行重新绑定）。
- **尚未真机确认**：中断记录能否按预期出现、开启无障碍辅助后划掉后台是否能被系统重新拉起。

## 1.1.0（多主播）

| 验证 | 结果 | 范围 |
| --- | --- | --- |
| 界面回归 | 40 个场景通过 | 在 1.0.5 的 30 个场景上新增 10 个：主播列表与独立开关、导航标签、识别后保存、修改房间号拦截、识别失败提示、删除确认、守候心跳中断提示、网络提示恢复后撤下、启动失败提示出现与撤下、页脚版本随桥接值变化 |
| 主播列表规则 | 46 个断言通过 | 默认值、上限 16、编号字符集与路径安全、名称长度、UID/房间号合法性、禁用保留、读取修复不抛异常 |
| 轮询与保活计时 | 72 个断言通过 | 唤醒锁随主播数增长且单主播仍为 25 秒、限流与普通失败的退避阶梯及上限、单轮耗时折算间隔、保活唤醒始终晚于本轮间隔且不低于 90 秒下限 |
| 既有规则回归 | 36,038 + 12 + 13 个断言通过 | 时间与场次去重、响铃策略、守候通知状态未受影响 |
| Java 结构自检 | 24 个文件括号与引号配平通过 | `tools/check_java_balance.py`；不是编译器 |

**尚未验证**：本版 Android 端 Java 已用 Android SDK 35（platform-35_r02）与 build-tools 35.0.0 完整编译通过，经 D8 转换、zipalign 对齐、apksigner v2/v3 签名校验通过，产物为 `build/ManquAlarm-multi-1.1.0.apk`（独立包名 `dev.hazel.livealarm.multi`，`versionCode=7`、`versionName=1.1.0`，应用名「VR闹钟」，可与原版并存）。**但尚未在真机上运行过**：多主播轮询、互斥响铃、头像下载与缓存、升级迁移、备份恢复、保活唤醒与看门狗均只经过编译与纯 Java 层测试，没有端到端执行。头像识别依赖 B 站 `get_anchor_in_room` 接口的当前返回结构，未在真实网络下验证（识别失败会退化为「主播 <房间号>」并允许手动改名，头像退回首字母）。

真机需重点复核：安装后与旧版并存是否互不干扰（两个应用同时开启守候会各响一次，建议只留一个）、多主播同时开播是否只响一次、识别失败时能否改名保存、缓存头像是否正常显示、加入名单后长时间不打开应用是否仍能按间隔检测。

**首次真机反馈与修复（2026-09-10）**：用户在真机上遇到两件事。

1. 「识别主播信息」报「操作未完成，请重试」。原因是 `Bridge.request` 把动作丢在 UI 线程执行，而该动作在主线程发起网络请求，抛出的 `NetworkOnMainThreadException` 消息为 null，被兜底文案掩盖。已改为在后台线程完成并延迟回包，且兜底文案在消息为空时带上异常类名。**此项已在真机确认修复。**
2. 主播开播没有提醒，事后打开应用才补上响铃；守候页长期显示「网络未连接」，但实际上已联网。查证结果：前者是**全天守候不安排任何定时唤醒**——`TimeRules.nextBoundary` 在 `allDay` 下返回 0，因此进程被系统杀掉后没有任何机制能唤醒它（新建的独立应用也不会继承旧版的“自启动/电池不限制”授权），已新增每 15 分钟的看门狗闹钟尝试自行恢复，并在守候页把心跳中断明确显示出来；后者是本版引入的回归——重写轮询循环时漏了「检测成功即清除 networkError」，已在 `apply()` 中补回，并让网络判断在 Wi-Fi/移动数据切换的瞬间不再误报断网。**这两项尚未在真机确认。**

**第二次全量复查（2026-09-11，1.1.0）**：针对“不要再出现这类问题”做了一次从头到尾的检查，并把发现的问题一并修掉。

| 发现 | 性质 | 处理 |
| --- | --- | --- |
| 检测循环跑在主线程消息队列上，没有任何唤醒能力 | 结构性，正是“整天没提醒”的根因 | 每轮检测结束后安排一次系统唤醒闹钟（本轮间隔的三倍，下限 90 秒）；循环正常时每轮把它推后，停住时才触发 |
| `apply()` 抛出的异常既没有捕获也没有重新排期 | 会导致整条轮询静默终止，或以未捕获异常结束进程 | 捕获后记入记录并按间隔重试；`busy` 在调用前就已复位，不会卡死 |
| 被互斥跳过的场次没有被标记为已提醒 | 与界面“不会叠着响”的承诺不符：第一位响铃一停，下轮就会接着响 | 跳过的场次标记为已处理，只记入记录 |
| 新增主播或改房间号会继承旧编号的场次状态 | 加入时已在播会被当成刚开播而误响；换房间号后旧场次可抑制新提醒 | 改动时重置该主播的场次与上次状态，并把加入时间记为当前 |
| `parse()` 同时比对房间号与主播 UID | 两个接口对同一房间的 UID 可能不同，一旦不一致该主播永久无法提醒 | 只按房间号校验，UID 仅用于跳转 |
| 三处写死的版本号（界面页脚、通知排查包 README、预览状态） | 版本更新后界面仍显示旧号 | 改为从已安装包信息读取 |
| 后台启动失败与“通知被拒”共用同一个提示位 | 启动失败的提示会一直挂在守候页 | 拆成两个字段，服务起来或检测成功即撤下；通知恢复可用时“通知被拒”提示也一并撤下 |
| 测试响铃期间的首次开播被当作重复丢弃 | 真实开播可能被一次 60 秒测试挡住 | 真实开播改为打断测试铃 |
| 填入不存在的房间号提示“B 站接口返回 -400，等待重试” | 文案误导 | 改为“没有找到这个直播间，请检查房间号” |

**仍未验证**：上述九项在真机上的实际表现。保活唤醒能否在用户的手机（小米/澎湃）上按预期触发、看门狗能否在被系统杀掉后成功拉起服务，取决于该机型的自启动与电池策略，应用无法自行验证，也无法突破系统强行停止。

## 1.0.5 已完成

| 验证 | 结果 | 范围 |
| --- | --- | --- |
| 1.0.5 界面回归 | 30 个场景通过 | 模拟 Android 通信下的选择刷新、异步竞态、权限展示、兼容同意、时区和通知状态 |
| 1.0.5 守候通知状态 | 13 个断言通过 | 服务状态、授权、通知通道和登记状态的组合 |
| 1.0.5 响铃策略 | 12 个断言通过 | 正常授权、拒绝、不一致及显式同意声音兼容 |
| 核心时间与场次规则 | 36,038 个断言通过 | 时间边界、跨日、每周规则、时区、夏令时、场次去重与补报；本次公开打包重新执行通过 |
| 原签名发行 APK | 已构建并核验签名 | 包名、版本、资源与打包内容；摘要见 `APK-SHA256.txt` |

可公开的 1.0.5 自动化记录保存在 [`verification/1.0.5/`](../verification/1.0.5/)。运行方法见 [BUILDING](BUILDING.md)。

## 真机反馈的适用范围

测试者反馈此前兼容版本在小米可以播放声音，另有 Vivo 正常使用反馈。针对一份 1.0.5 系统报告，确认前台守候服务正在运行，并发现设备管理策略固定拒绝通知；这不能代表所有小米设备的原因或完整兼容性。

原生测试 APK 已完成编译，没有在本项目验证环境的真实手机上执行。浏览器模拟通信也不等于实际 Android 服务、声音或锁屏行为。

## 尚未完成

- 所有支持的 Android 版本与厂商机型上的安装、授权、锁屏响铃、通知操作和恢复测试。
- 解除设备管理策略后系统通知开关持续保持的真机复验。
- 长时间待机耗电、网络中断、系统杀进程、后台限制与真实开播端到端验证。
- 当前 GitHub 环境的完整 Gradle / CI 构建。

因此本项目不承诺零漏报、秒级必达或永久保活。
